/*
 * Source: BuscaempBcaCaller.java - Generated by OBCOM SQL Wizard 1.158
 * Author: Cesar Luis Gonzalez-Rubio Chacin (Imperial S.A.)
 *
 * Copyright (c) IMPERIAL S.A. All rights reserved.
 * 
 * All  rights  to  this product are owned by IMPERIAL S.A. and may only be used
 * under the terms of its associated license document. You may NOT copy, modify,
 * sublicense,  or  distribute  this  source  file  or  portions  of  it  unless
 * previously authorized in writing by IMPERIAL S.A. In any event,  this  notice
 * and above copyright must always be included verbatim with this file.
 */

package cl.imperial.estacionmantencion.ws;

import java.sql.CallableStatement;
import java.sql.Connection;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Types;
import java.util.ArrayList;
import java.util.List;
import javax.sql.DataSource;

/**
 * Caller of procedure {@code EstacionMantencion$BuscaEmp_bca}.
 *
 * @author Cesar Luis Gonzalez-Rubio Chacin (Imperial S.A.)
 */
public class BuscaempBcaCaller extends ProcedureCaller
{
    //--------------------------------------------------------------------------
    //-- Constructor Methods ---------------------------------------------------
    //--------------------------------------------------------------------------

    /**
     * Constructs a new {@code BuscaempBcaCaller} instance.
     */
    public BuscaempBcaCaller()
    {
    }

    //--------------------------------------------------------------------------
    //-- Execute Methods -------------------------------------------------------
    //--------------------------------------------------------------------------

    /**
     * Executes procedure {@code EstacionMantencion$BuscaEmp_bca} using a data source.
     *
     * @param  dataSource the connection data source.
     * @return the output parameters and result sets of the procedure.
     * @throws SQLException if an SQL error occurs.
     */
    public BuscaempBcaResult executeProc(final DataSource dataSource)
        throws SQLException
    {
        try (final Connection conn = dataSource.getConnection()) {
            return executeProc(conn);
        }
    }

    /**
     * Executes procedure {@code EstacionMantencion$BuscaEmp_bca} using a connection.
     *
     * @param  conn the database connection.
     * @return the output parameters and result sets of the procedure.
     * @throws SQLException if an SQL error occurs.
     */
    public BuscaempBcaResult executeProc(final Connection conn)
        throws SQLException
    {
        final BuscaempBcaResult result = createProcResult();
        final String jdbcURL = getJdbcURL(conn);
        if (jdbcURL.startsWith("jdbc:oracle:")) {
            try (final CallableStatement call = prepareCall(conn, "{call EstacionMantencion$BuscaEmp_bca(?,?)}")) {
                call.registerOutParameter(1, Types.NVARCHAR);
                call.registerOutParameter(2, ORACLE_CURSOR);
                call.execute();
                result.setErrorMessage(call.getString(1));
                result.setEstaciones(readEstaciones((ResultSet) call.getObject(2)));
            }
        } else if (jdbcURL.startsWith("jdbc:postgresql:")) {
            try (final CallableStatement call = prepareCall(conn, "{call EstacionMantencion$BuscaEmp_bca(?,?)}")) {
                call.registerOutParameter(1, Types.NVARCHAR);
                call.registerOutParameter(2, Types.OTHER);
                call.execute();
                result.setErrorMessage(call.getString(1));
                result.setEstaciones(readEstaciones((ResultSet) call.getObject(2)));
            }
        } else {
            try (final CallableStatement call = prepareCall(conn, "{call EstacionMantencion$BuscaEmp_bca(?)}")) {
                call.registerOutParameter(1, Types.NVARCHAR);
                int updateCount = 0;
                boolean haveRset = call.execute();
                while (haveRset || updateCount != -1) {
                    if (!haveRset) {
                        updateCount = call.getUpdateCount();
                    } else if (result.getEstaciones() == null) {
                        result.setEstaciones(readEstaciones(call.getResultSet()));
                    } else {
                        unexpectedResultSet(call.getResultSet());
                    }
                    haveRset = call.getMoreResults();
                }
                result.setErrorMessage(call.getString(1));
            }
        }
        return result;
    }

    /**
     * Creates and returns a new instance of {@code BuscaempBcaResult}.
     * <p>
     * Can be overridden to return a subtype of {@code BuscaempBcaResult}.
     *
     * @return a new {@code BuscaempBcaResult} instance.
     */
    protected BuscaempBcaResult createProcResult()
    {
        return new BuscaempBcaResult();
    }

    //--------------------------------------------------------------------------
    //-- BuscaempBcaEstacion ResultSet Methods ---------------------------------
    //--------------------------------------------------------------------------

    /**
     * Converts a result set to an array of {@code BuscaempBcaEstacion}.
     *
     * @param  resultSet the result set to convert (can be null).
     * @return an array of {@code BuscaempBcaEstacion} or null.
     * @throws SQLException if an SQL error occurs.
     */
    protected List<BuscaempBcaEstacion> readEstaciones(final ResultSet resultSet)
        throws SQLException
    {
        if (resultSet == null)
            return null;
        try {
            // Obtain ordinal (index) numbers of result columns
            final int cCodEmp = resultSet.findColumn("COD_EMP");
            final int cNomEmp = resultSet.findColumn("NOM_EMP");

            // Convert result rows to an array of "BuscaempBcaEstacion"
            final List<BuscaempBcaEstacion> list = new ArrayList<>();
            while (resultSet.next()) {
                final BuscaempBcaEstacion item = createEstacion();
                item.setCodEmp(resultSet.getString(cCodEmp));
                item.setNomEmp(resultSet.getString(cNomEmp));
                if (filterEstacion(item)) list.add(item);
            }
            return list;
        } finally {
            resultSet.close();
        }
    }

    /**
     * Creates and returns a new instance of {@code BuscaempBcaEstacion}.
     * <p>
     * Can be overridden to return a subtype of {@code BuscaempBcaEstacion}.
     *
     * @return a new {@code BuscaempBcaEstacion} instance.
     */
    protected BuscaempBcaEstacion createEstacion()
    {
        return new BuscaempBcaEstacion();
    }

    /**
     * Returns {@code true} if supplied item should be added to result set.
     * <p>
     * Can be overridden to select which items are added to the result set.
     *
     * @param  item the {@code BuscaempBcaEstacion} to be checked.
     * @return {@code true} if {@code item} should be added to result set.
     * @throws NullPointerException if {@code item} is {@code null}.
     */
    protected boolean filterEstacion(final BuscaempBcaEstacion item)
    {
        return true;
    }

    //--------------------------------------------------------------------------
    //-- Static Methods --------------------------------------------------------
    //--------------------------------------------------------------------------

    /**
     * Executes procedure {@code EstacionMantencion$BuscaEmp_bca} using a data source.
     *
     * @param  dataSource the connection data source.
     * @return the output parameters and result sets of the procedure.
     * @throws SQLException if an SQL error occurs.
     */
    public static BuscaempBcaResult execute(final DataSource dataSource)
        throws SQLException
    {
        return new BuscaempBcaCaller().executeProc(dataSource);
    }

    /**
     * Executes procedure {@code EstacionMantencion$BuscaEmp_bca} using a connection.
     *
     * @param  conn the database connection.
     * @return the output parameters and result sets of the procedure.
     * @throws SQLException if an SQL error occurs.
     */
    public static BuscaempBcaResult execute(final Connection conn)
        throws SQLException
    {
        return new BuscaempBcaCaller().executeProc(conn);
    }
}
